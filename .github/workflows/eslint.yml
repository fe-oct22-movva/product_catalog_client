name: Lint
on: [pull_request]
jobs:
  eslint:
    name: runner / eslint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write 
    steps:
      - uses: actions/checkout@v2
      - uses: reviewdog/action-eslint@v1
        with:
          github_token: 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
          reporter: github-check
          eslint_flags: 'src/'